public class MiscellaneousTest {

    //browser navigation - driver.navigate;

    //shadow DOM
    //webElement.getShadowRoot();
    // search as usual

    //cookies driver.manage.* - example

    //listeners

    //exceptions

    //headless
    //ChromeOptions options = new ChromeOptions();
    //options.setHeadless(true);
    //driver = new ChromeDriver(options)
    //
    //@Test
    //void testHeadless(@Arguments("--headless") ChromeDriver driver) {

    //mobile device emulation
    //ChromeOptions options = new ChromeOptions();
    // Map<String, Object> mobileEmulation = new HashMap<>();
    // mobileEmulation.put("deviceName", "iPhone 6/7/8");
    // options.setExperimentalOption("mobileEmulation", mobileEmulation);

    //can install extensions (file only)
    //geolocation, notification

    //proxy
    //Proxy proxy = new Proxy();
    // String proxyStr = "proxyIP:proxyPort";
    // proxy.setHttpProxy(proxyStr);
    // proxy.setSslProxy(proxyStr);
    // ChromeOptions options = new ChromeOptions();
    // options.setAcceptInsecureCerts(true);
    // options.setProxy(proxy);

    //print web page to PDF

    // remote run
    // parallel run
}
